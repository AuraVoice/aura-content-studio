2026-07-28
Problem: ESLint 9 failed before linting with "Converting circular structure to JSON".
Issue: Next 16 exports native flat configurations, but wrapping them with FlatCompat made ESLint's config validator traverse a circular plugin graph.
Solution: Import eslint-config-next/core-web-vitals and eslint-config-next/typescript directly and compose them with defineConfig.
Justification: The native flat exports match ESLint 9 and Next 16, avoid compatibility serialization, and keep the same rule coverage.

2026-07-28
Problem: The latest Next 16 install reported high-severity 2026 advisories in its transitive PostCSS and Sharp packages, plus glob-matching advisories in development tools.
Issue: npm's suggested forced fix incorrectly proposed downgrading Next to 9.3.3 and using breaking tool changes.
Solution: Pin patched transitive releases with npm overrides, remove the unused coverage package, then rerun lint, tests, build, and audit.
Justification: This keeps the current framework, closes the vulnerable dependency paths, and verifies compatibility instead of accepting npm's unsafe downgrade.

2026-07-28
Problem: A camera-only revision could preserve global locks while still allowing visual, dialogue, overlay, and timing details inside each shot to drift.
Issue: Locked attributes and shot-level fields are separate structures, so copying only lockedAttributes was incomplete.
Solution: Merge only the generated camera field into the prior shot plan and retain all other shot fields plus negative constraints, model, and failure watch.
Justification: "Change only" is a structural invariant and must be enforced in code after model output, not trusted to prompting alone.

2026-07-28
Problem: The initial credential design added a separate session secret and a second search vendor even though this is a one-owner tool and Brave Search is already used by the wider Aura product.
Issue: Extra independent secrets and providers increase setup and rotation work without adding useful isolation at this scale.
Solution: Derive the JWT signing key with scrypt from the strong studio password and replace Tavily with the Brave Search Web API plus local mocks.
Justification: Password rotation now invalidates all sessions automatically, and the application reuses the existing search vendor while keeping server-side key isolation.

2026-07-28
Problem: A private bucket was serving videos through temporary signed URLs embedded in dashboard state, and login had no online guessing controls.
Issue: Anyone who obtained a signed URL could use it until expiry, while unlimited login attempts increased password-guessing exposure.
Solution: Replace signed URLs with an authenticated same-origin media route, move to random opaque database sessions, add IP and account lockouts, revoke anonymous database function access, and disable caching.
Justification: Authorization is now checked at the moment each video is requested, leaked media paths are useless without the owner session, and online password guessing is bounded.

2026-07-29
Problem: The desktop dashboard navigation worked, but the page could not scroll beyond the first viewport.
Issue: The viewport-height dashboard clipped overflow while its nested workspace grid kept the default automatic minimum height, so the content row expanded instead of becoming a scroll container.
Solution: Allow the workspace and content row to shrink with min-height zero, contain desktop overflow in the intended content scroller, and restore visible overflow on compact layouts.
Justification: The dashboard now has one reliable desktop scroll viewport while mobile keeps normal document scrolling and the design system's hidden-scrollbar behavior.

2026-07-29
Problem: Daily campaign research was scheduled for 16:00 UTC indefinitely, which is 9:00 AM Pacific during daylight saving time and had no 30-day stop condition.
Issue: Vercel cron expressions are always UTC and do not support an end date, so the endpoint needed its own campaign window guard.
Solution: Move the cron to 15:00 UTC and allow campaign work only from July 29 through August 27, 2026, inclusive.
Justification: The active window contains exactly 30 Pacific calendar dates, and calls outside it stop before database or provider work.

2026-07-29
Problem: Trend Scout failures stopped daily and manual research without sending the owner a Telegram message, while simultaneous search-provider failures were reported only as missing research.
Issue: Specialist exceptions escaped the LangGraph orchestrator, Telegram delivery happened only after a successful workflow return, and Promise.allSettled discarded every rejected search reason.
Solution: Convert Trend Scout exceptions into handled workflow state, preserve unique provider errors, and run scheduled and manual research inside a durable workflow that creates versioned attempt claims and retries with capped backoff until Telegram confirms delivery.
Justification: The orchestrator now owns specialist failure behavior, failed attempts remain diagnosable without ending the job, and function timeouts or deployments cannot silently discard the pending Telegram result.

2026-07-30
Problem: Brave Search rate limits and Gemini 429 responses could prevent the workflow from ever reaching a usable video-generation prompt.
Issue: Five Brave requests started concurrently despite request-rate enforcement, empty or failed search results were treated as terminal, and Prompt Director had no rate-limit fallback.
Solution: Pace and retry Brave requests, continue with product-safe evergreen ideas when optional research or Trend Scout fails, and return a validated three-clip production package when Prompt Director is rate limited.
Justification: Web research now improves an idea without becoming a prerequisite, while the highest-value output, a directly usable video prompt, remains available during provider throttling.
